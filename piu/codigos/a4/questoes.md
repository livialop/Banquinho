# Questões

**Q1** – Escreva um programa que identifique se os números de um array são pares ou ímpares (considere 0 par). Crie uma função parOuImpar(numeros) que recebe um array de números e exibe, para cada número, se ele é par ou ímpar. Combine o método forEach do array com uma função anônima.

Ex.:

```javascript
parOuImpar([1, 2, 5, 6, 0]);
/* Exibe:
1 é ímpar
2 é par
5 é ímpar
6 é par
0 é par */
```

**Q2** – Modifique o programa da questão anterior usando uma função arrow (que usa a sintaxe `() => {}` ) no lugar da função anônima.

**Q3** – Escreva um programa que dobre os números de um array. Crie uma função dobrar(numeros) que recebe um array de números e retorna um novo array contendo o dobro de cada número. Combine o método map do array com uma função anônima.

Ex.:

```javascript
dobrar([1, 2, 2, 3, 5, 6]) // retorna [2, 4, 4, 6, 10, 12]
```

**Q4** – Modifique o programa da questão anterior para que a função seja multiplicar(fator, numeros) e multiplique os números pelo fator.

Ex.:

```javascript
multiplicar(3, [1, 2, 2, 3, 5, 6]) // retorna [3, 6, 6, 9, 15, 18]
```

**Q5** – Modifique o programa da questão anterior usando uma função arrow (que usa a sintaxe `() => {}` ) no lugar da função anônima.

**Q6** – Escreva um programa que filtre os números pares de um array. Crie uma função filtrarPares(numeros) que recebe um array de números e retorna um novo array contendo apenas os números pares. Combine o método filter do array com uma função anônima.

Ex.:

```javascript
filtrarPares([1, 2, 2, 3, 5, 6]) // retorna [2, 2, 6]
```

**Q7** – Modifique o programa da questão anterior para que a função seja filtrar(divisor, numeros) e filtre os números divisíveis por divisor do array.

Ex.:

```javascript
filtrar(3, [1, 2, 2, 3, 5, 6]) // retorna [3, 6]
```

**Q8** – Modifique o programa da questão anterior usando uma função arrow (que usa a sintaxe `() => {}` ) no lugar da função anônima.

**Q9** – Escreva um programa que some os números de um array. Crie uma função somar(numeros) que recebe um array de números e retorna a soma. Combine o método reduce do array com uma função anônima.

Ex.:

```javascript
somar([1, 2, 2, 3, 5, 6]) // retorna 19
```

**Q1**0 – Modifique o programa da questão anterior usando uma função arrow (que usa a sintaxe `() => {}` ) no lugar da função anônima.
